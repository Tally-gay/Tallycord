import { Devs } from "@utils/constants";
import definePlugin from "@utils/types";

export default definePlugin({
    authors: [Devs.tally],
    name: "Better Window Bar",
    description: "A plugin that improves the window bar by adding the freaking close and minimize buttons.",
    patches: [

    ]
});
